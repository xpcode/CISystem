const initialState = {
    isLoggedIn: false,
    id: null,
    name: null,
}

const user = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default user