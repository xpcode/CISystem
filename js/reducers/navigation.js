const initialState = {
    tab: 'home'
}

const navigation = (state = initialState, action) => {
    switch (action.type) {
        case 1:
            break;

        default:
            return state
    }
}

export default navigation